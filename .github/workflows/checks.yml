---
name: Checks

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  test-lint:
    name: Test and lint
    runs-on: ubuntu-24.04

    if: |
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.head_commit.message, '[run-ci]') ||
      contains(github.event.pull_request.title, '[run-ci]')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Loging to docker hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and Cache
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: boardgames-app:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Tests
        run: docker compose run --rm app sh -c "python manage.py wait_for_db && python manage.py test"
      - name: Lint
        run: docker compose run --rm app sh -c "ruff check . --no-cache"
